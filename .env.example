# ═══════════════════════════════════════════════════════════
# ATS Resume App — Environment Variables
# ═══════════════════════════════════════════════════════════
# Copy this file to .env and fill in your API key(s).
#
# You need AT LEAST ONE of these:
#   Option A (FREE):  GEMINI_API_KEY  → Google Gemini 2.0 Flash
#   Option B (PAID):  OPENAI_API_KEY  → OpenAI GPT-4o-mini
#   Option C (BOTH):  Set both → Gemini primary, OpenAI fallback
#   Option D (NONE):  App still works with basic heuristic parsing (no LLM)
# ═══════════════════════════════════════════════════════════

# ── Option A: Google Gemini (FREE — recommended for development) ──
# Get your free API key at: https://aistudio.google.com/apikey
# Free tier: 15 requests/min, 1M tokens/day, 1500 requests/day
GEMINI_API_KEY=

# ── Option B: OpenAI (PAID — recommended for production) ──
# Get your API key at: https://platform.openai.com/api-keys
# Cost: ~$0.15/1M input tokens, ~$0.60/1M output tokens (GPT-4o-mini)
OPENAI_API_KEY=
